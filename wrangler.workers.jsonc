{
  "$schema": "node_modules/wrangler/config-schema.json",
  // Workers / OpenNext CLI deploy (`npm run deploy` / `npm run upload`).
  "name": "hub-of-craftss",
  "main": ".open-next/worker.js",
  // Keep minify on for Workers Free 3 MiB gzip limit. (Unminified ~3.4 MiB gzip fails.)
  "minify": true,
  "compatibility_date": "2025-05-05",
  "compatibility_flags": ["nodejs_compat", "global_fetch_strictly_public"],
  "assets": {
    "directory": ".open-next/assets",
    "binding": "ASSETS",
    // false = static assets served directly by Cloudflare (not billed, no Worker
    // CPU). Middleware matcher already excludes static paths, so this is safe.
    "run_worker_first": false
  },
  "services": [
    {
      "binding": "WORKER_SELF_REFERENCE",
      "service": "hub-of-craftss"
    }
  ],
  // Native R2 access for Worker put/get/delete (avoids S3 API 401s from aws4fetch).
  "r2_buckets": [
    {
      "binding": "MEDIA_BUCKET",
      "bucket_name": "hubofcraftss-cdn"
    }
  ],
  "observability": {
    "enabled": true,
    "head_sampling_rate": 1
  }
}
