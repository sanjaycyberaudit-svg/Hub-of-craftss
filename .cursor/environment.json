{
  "name": "Hub of craftss",
  "user": "ubuntu",
  "install": "bash .cursor/cloud-agent-install.sh",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev",
      "description": "Next.js dev server (http://localhost:3000). Renders the storefront; store data sections show a fallback until real Supabase/DB secrets are added."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
